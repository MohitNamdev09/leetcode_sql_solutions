with final_table as (select u.name , nvl(sum(r.distance),0) as travelled_distance, u.id from users u left join rides r on u.id = r.user_id
group by u.id,u.name
order by nvl(sum(r.distance),0)  desc,
u.name asc)
select name, travelled_distance from final_table;
